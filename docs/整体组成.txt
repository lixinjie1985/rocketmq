一个MQ集群由多个broker组成，所有broker的集群名称都相同
一个broker由多个节点组成，所有节点的broker名称都相同，其中brokerId为0的节点自动成为主节点，其余brokerId为非0的节点自动成为从节点
一个broker中的每个节点都可以有多个队列，主节点中的主队列可以接受读写请求，从节点中的从队列只接受读请求，从节点是主节点的备份
一个topic可以对应于多个broker中的多个队列，消息可以循环地发送给多个broker中的多个队列，也可以进行细粒度的控制来选择发送给特定的某个队列